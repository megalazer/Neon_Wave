export const EVENT_COOLDOWN = 4;   // min turns between choice events
export const FLAVOR_MIN_GAP = 3;   // earliest a flavor event can fire
export const FLAVOR_MAX_GAP = 7;   // forces a flavor event if exceeded
export const FLAVOR_CHANCE  = 0.4; // prob per turn inside the min–max window
